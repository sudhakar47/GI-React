/*
 *
 * HomePageContainer actions
 *
 */

import { DEFAULT_ACTION } from '../../constants/homePageContainer/constants';

export function defaultAction() {
  return {
    type: DEFAULT_ACTION,
  };
}
