/*
 * HeaderSearch Messages
 *
 * This contains all the text for the HeaderSearch container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HeaderSearch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderSearch container!',
  },
});
