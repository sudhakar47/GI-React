/*
 * FeaturedLinkBar Messages
 *
 * This contains all the text for the FeaturedLinkBar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FeaturedLinkBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FeaturedLinkBar component!',
  },
});
